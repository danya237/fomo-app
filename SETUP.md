# 🚀 Інструкція по налаштуванню MovieSwipe

## ⚠️ ВАЖЛИВО: Перед запуском

### 1. Отримати API ключ TMDb

Це **обов'язково** для роботи додатку!

1. Зайдіть на https://www.themoviedb.org/
2. Створіть безкоштовний акаунт
3. Перейдіть в Settings → API
4. Створіть новий API ключ (v3 auth)
5. Скопіюйте ключ

### 2. Додати API ключ в код

Відкрийте файл `src/services/tmdb.ts` і замініть:

```typescript
const TMDB_API_KEY = 'YOUR_TMDB_API_KEY_HERE';
```

на

```typescript
const TMDB_API_KEY = 'ваш_справжній_ключ_тут';
```

## 🏃 Запуск додатку

### Варіант 1: Запуск на емуляторі/симуляторі

```bash
# Встановіть залежності (якщо ще не встановили)
npm install

# Запустіть Expo
npx expo start

# Потім натисніть:
# - 'i' для iOS симулятора (тільки на macOS)
# - 'a' для Android емулятора
# - 'w' для веб-версії
```

### Варіант 2: Запуск на реальному телефоні

1. Встановіть Expo Go з App Store (iOS) або Google Play (Android)
2. Запустіть `npx expo start`
3. Відскануйте QR код з терміналу камерою телефону
4. Додаток відкриється в Expo Go

## 📱 Тестування функцій

### Після запуску перевірте:

1. ✅ Свайп фільмів вправо (лайк) та вліво (дизлайк)
2. ✅ Збереження лайканих фільмів в розділі "Вподобані"
3. ✅ Перегляд трейлерів
4. ✅ Зміна мови в налаштуваннях (UA/EN/NO)
5. ✅ Зміна теми (світла/темна)

## 🔧 Можливі проблеми

### Проблема: "Network request failed"
**Рішення**: Переконайтеся що додали API ключ TMDb

### Проблема: Білий екран при запуску
**Рішення**: 
```bash
npm start -- --clear
```

### Проблема: Помилка з Metro bundler
**Рішення**:
```bash
npx expo start -c
```

## 🎯 Наступні кроки

1. **Firebase** (для друзів та синхронізації):
   - Створіть проєкт на https://firebase.google.com/
   - Додайте конфігурацію в `src/services/firebase.ts`
   - Встановіть: `npm install firebase`

2. **Додаткові функції**:
   - Пошук фільмів
   - Фільтри по жанрах
   - Рекомендаційний алгоритм
   - Система друзів

## 📞 Потрібна допомога?

Якщо щось не працює:
1. Перевірте чи встановлений API ключ
2. Перевірте інтернет-з'єднання
3. Спробуйте перезапустити Metro bundler
4. Очистіть кеш: `npx expo start -c`

---

Успіхів! 🎬🍿
